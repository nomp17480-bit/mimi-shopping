import {createClient} from '@/utils/supabase/server'; export default async function Home(){const s=await createClient();const {data:c=[]}=await s.from('categories').select('*');const {data:p=[]}=await s.from('products').select('*');return <><header><b>Mimi Shopping 👜</b></header><main className='wrap'><section className='hero'><h1>أناقة تتجدد مع كل موسم ✨</h1></section><h2>الأقسام</h2>{c.map((x:any)=><div className='card' key={x.id}>{x.icon} {x.name}</div>)}<h2>المنتجات</h2>{p.map((x:any)=><div className='card' key={x.id}><b>{x.name}</b><p>{x.price} دج</p></div>)}</main></>}